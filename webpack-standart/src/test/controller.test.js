import Controller from './controller';

describe("controller", () => {
	it("exists", () => {
		expect(Controller).to.exist
	});
	it("works again", () => {
		//
		//throw new Error("failure!");
	});
});
